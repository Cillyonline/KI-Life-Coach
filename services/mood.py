"""Mood tracking services."""


def record_mood(user_id: int, mood: int) -> None:
    """Record a user's mood.

    Args:
        user_id: Unique user identifier.
        mood: Mood value on a predefined scale.
    """
    del user_id, mood  # Placeholder implementation
