"""Habit tracking services."""


def add_habit(user_id: int, habit: str) -> None:
    """Add a habit for a user.

    Args:
        user_id: Unique user identifier.
        habit: Habit description.
    """
    del user_id, habit  # Placeholder implementation
